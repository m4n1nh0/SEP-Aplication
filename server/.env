PORT=3001
NODE_ENV=development

# Banco de dados
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root123
DB_NAME=sep_db

# JWT
JWT_SECRET=KHDSLKJHUIY983729KJHSJKHJKHKSLA
JWT_EXPIRES_IN=8h

# Segurança
MAX_LOGIN_ATTEMPTS=5
LOCK_TIME_MINUTES=30
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
AUTH_RATE_LIMIT_MAX=10

# Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=5

# Email (configure para produção)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu@email.com
SMTP_PASS=sua_senha_app
EMAIL_FROM=SEP Estácio <noreply@sep.estacio.br>

# App URL
CLIENT_URL=http://localhost:5173
APP_URL=http://localhost:3001

# ── AWS S3 ─────────────────────────────────────────────────
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=seu_access_key_aqui
AWS_SECRET_ACCESS_KEY=seu_secret_key_aqui
AWS_S3_BUCKET=sep-prontuarios
# Tempo de expiração das URLs presigned (segundos) — padrão: 900 = 15 min
S3_URL_TTL_SECONDS=900

# ── Domínios institucionais (e-mail obrigatório para usuários internos) ──────
# Deixe vazio para não validar. Separe múltiplos domínios por vírgula.
EMAIL_DOMINIO_COORDENADOR=estacio.br
EMAIL_DOMINIO_SUPERVISOR=professore.estacio.br
EMAIL_DOMINIO_ESTAGIARIO=alunos.estacio.br